# 技术决策记录（DECISIONS.md）

> 记录每个关键选型的"为什么"。面试官问到任何一条，答案都在这里。

## 1. LLM：为什么用 DeepSeek 而不是 OpenAI / 本地模型？

- 国内网络直连稳定，无需代理；价格约为 GPT-4 级模型的 1/20，学生项目可承受
- OpenAI 兼容接口：`base_url` 一行即可切换任意厂商（config.py），不锁定供应商
- 不用本地模型：消费级显卡跑 7B 模型的指令遵循能力不足以稳定输出结构化 JSON

## 2. OCR：为什么用 RapidOCR 而不是调用多模态大模型？

- 离线、免费、毫秒级：招聘截图是标准印刷体，OCR 准确率已够用，识别错误交给下游 LLM 纠错（prompt 里明确要求纠正 OCR 错字）
- 多模态大模型（如 GLM-4V）按图计费且延迟高；本场景"OCR + 文本 LLM"分层方案成本低一个数量级
- trade-off：版面复杂的图（表格海报）OCR 会丢结构，此时应升级多模态——已留在 Roadmap

## 3. Agent 编排：为什么用 LangGraph 而不是 Dify / 裸写链式调用？

- vs Dify：匹配流程需要"结构化中间态"（画像 JSON → 评分 JSON → 流式文本），Dify 可视化编排拿不到这种细粒度的状态控制和自定义 SSE 事件
- vs 裸写：LangGraph 的 StateGraph 让节点显式声明读写状态，后续加分支（如"分数太低直接终止"）、重试、checkpoint 持久化不用重构
- 三节点拆分而非一次 prompt 全要：每步输出可单独校验（Pydantic），失败可定位；单 prompt 输出长 JSON 的截断/格式错误率明显更高

## 4. 向量库：为什么用 Chroma 而不是 Milvus / FAISS？

- 数据规模是百级文档、千级切块——Milvus 的分布式能力在此毫无收益，运维成本却是实打实的（etcd + MinIO + 独立进程）
- vs FAISS：FAISS 是索引库不是数据库，删除/元数据过滤/持久化都要自己写；Chroma 内置这些且 API 简单
- 何时该换：文档到十万级、需要多副本高可用时换 Milvus——预留了 kb.py 单文件抽象，切换只改一个模块

## 5. Embedding：为什么本地 BGE 而不是 API？

- DeepSeek 没有 embedding 接口；引入第二家 API 增加一个密钥依赖和计费点
- bge-small-zh-v1.5（~100MB）中文检索效果在 C-MTEB 有公开基准，CPU 推理毫秒级，离线可用
- 查询侧加 bge 官方指令前缀（"为这个句子生成表示…"），文档侧不加——遵循模型训练时的非对称设计

## 6. 检索策略：为什么混合检索（向量 + BM25 + RRF）？

- JD 文本的特点是**精确术语密集**（"LangGraph"、"六级"、"Milvus"）：纯向量检索对这类罕见 token 不敏感，BM25 恰好擅长
- 语义问题（"哪些岗位对英语有要求" → "英语六级以上"）靠向量通道
- 融合用 RRF（k=60）而不是加权分数：两路分数量纲不同（余弦相似度 vs BM25 分数），直接加权需要调参且不稳定；RRF 只用排名，无参数、对异常分数鲁棒
- 未做 Reranker：候选集只有 top15，交叉编码器重排的收益（NDCG 提升）在此规模下小于其延迟成本；数据量上去后是第一个该加的东西

## 7. 流式输出：为什么 SSE 而不是 WebSocket？

- 匹配/问答是**单向推送**场景（服务端→客户端），SSE 语义精确匹配；WebSocket 的双向能力用不上，还要自己处理心跳、重连、消息帧
- SSE 走标准 HTTP：过 Next.js rewrites 代理零配置，未来上 Nginx/CDN 也无特殊要求
- 踩过的坑：sse-starlette 用 `\r\n` 换行，前端解析器最初只按 `\n\n` 切帧导致所有事件被吞——修复见 commit 历史，帧解析必须兼容两种换行

## 8. 切块策略：为什么 400 字符段落聚合？

- JD/面经以短段落为主，400 字能容纳一个完整语义单元（一组技能要求/一段职责）
- 按段落边界聚合而不是固定窗口硬切：避免把"任职要求"从中间切断；仅对超长段落退化为带 50 字重叠的硬切
- chunk 太大（1000+）会稀释向量语义、塞爆 prompt；太小（100）会丢上下文——400 是针对本语料的经验值，评测脚本可验证（见 EVALS.md）

## 9. 数据库：为什么 SQLite 而不是 PostgreSQL？

- 单用户桌面场景，无并发写压力；SQLite 零部署，`git clone` 后即可跑
- SQLModel 抽象了方言，迁移 PostgreSQL 只改连接串
- BM25 语料直接从 SQLite 全量加载进内存重建（百级文档毫秒级），不需要 Elasticsearch

## 10. 数据接入：为什么"截图 OCR + 单页 URL 抓取"而不是爬虫？

- Boss 直聘等平台登录墙 + 反爬对抗强，批量爬取有封号与法律风险（有判例）；求职场景也不需要全量数据，只需要用户真想投的几十个
- URL 导入用 trafilatura 抽正文而不是 requests+正则：它自动剔除导航/页脚/广告，对未知站点泛化好
- 抓取入口做了基础 SSRF 防护：仅 http/https、DNS 解析后拒绝内网/回环地址——用户输入的 URL 永远当不可信数据处理
- 两条入口在 `_parse_and_save` 汇合走同一管线：新增数据源只需产出"纯文本"，下游零改动

## 11. 模拟面试：为什么多轮状态存 SQLite 而不用 LangGraph checkpointer？

- 面试是**线性**多轮问答：出题 → 回答 → 点评 → 下一题，没有分支/回退/并行，LangGraph 的图能力和 interrupt 机制在此没有收益，只增加一层抽象
- 轮次以 JSON 存进 `InterviewSession.rounds_json`：天然支持"回看整场面试"，调试时直接查库就能复现任何一轮的上下文
- 每轮两次 LLM 调用（流式点评 + JSON 出题）而不是一次调用输出两段：流式文本和结构化 JSON 的输出格式互斥，拆开各自用最合适的模式，也便于单独重试
- 何时该换 checkpointer：如果加入"追问分支"（答得差自动降低难度、答得好深入追问的树状路径），状态图就值得引入了

## 12. 代码仓库摄取：为什么按"文件+行窗口"切块而不是 AST？

- 面试场景的检索粒度是"这个文件里这段实现"，50 行窗口 + 文件路径前缀已让面试官能引用到具体文件和函数；AST 按函数切更精确，但要为每种语言维护解析器，收益暂不匹配成本
- 仓库概览（LLM 生成的技术栈/模块/数据流）固定为块 0：每轮出题都带全局视野，具体代码片段则按上一轮话题检索——"全局概览 + 局部检索"的组合避免把整个仓库塞进 prompt
- 检索支持 doc_id 过滤：面试时只搜该仓库的块，不会被 JD/面经的块干扰
- 文件优先级启发式（README > 源码 > 配置）+ 数量/大小上限：控制摄取成本，防超大仓库拖垮嵌入

## 13. 问答评测：为什么 LLM-as-Judge 做"主张级证据核对"而不是打总分？

- 让裁判直接给回答打 0-100 分，分数没有可解释性也不稳定；拆成**逐条事实主张 → 逐条核对资料证据**，每个判定都可人工复核，指标（忠实度=有据主张占比）有明确定义
- 陷阱题（知识库中不存在的信息）单独统计**拒答正确率**——这是 RAG 防幻觉的关键指标，只测可回答题会高估系统
- 已知局限：裁判与生成同为 DeepSeek，存在自评偏置。缓解：裁判只看【资料+回答】不看问题生成过程，任务限定为客观的证据核对；更严格的做法是换第二家模型做裁判（成本允许时的升级项）
- 踩过的坑：初版裁判把"资料中没有 X"的正确拒答误判为无据主张——拒答表述必须显式排除在事实主张之外




